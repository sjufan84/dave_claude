""" This file contains all the dependencies for the app. """
# import os
'''import json
from google.oauth2 import service_account
from dotenv import load_dotenv
import streamlit as st

# Load environment variables
load_dotenv()

def get_google_vision_credentials():
    """ Function to get the Google Vision credentials from an environment variable. """
    try:
      credentials = service_account.Credentials.from_service_account_info(json.loads(st.secrets["secrets"]["GOOGLE_CREDS"]))
      return credentials
    except Exception as e:
      print(e)'''
